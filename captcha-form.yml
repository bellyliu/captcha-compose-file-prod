version: '3'
services:

  app:
    image: dekribellyliu/captcha-webapp:1.3
    ports:
      - 80:80
    deploy:
      replicas: 1
      placement:
        constraints: [node.role == manager]
    networks:
      - app

networks:
  app: